import {NavLink} from 'react-router-dom';

const NavLogout = () => {
    return (
        <>
            <NavLink className='nav-link' to={'logowanie'}>
                logowanie
            </NavLink>
        </>
    );
};

export default NavLogout;
