import {NavLink} from 'react-router-dom';

const NavLogin= () => {
    return (
        <>
            <NavLink className='nav-link' to={'profil'}>
                profil
            </NavLink>
        </>
    );
};

export default NavLogin;
